# <子项目名> · 局部协作约定(Claude Code / Codex 共用入口)

> 本文件只适用于 `<子项目路径>/` 及其子目录。
> 总体项目合同见 `<ROOT_AGENTS_PATH>`;总体计划、交接和文档分别见 `<ROOT_FLOW_PATH>`、`<ROOT_DOCS_PATH>`。
> 同目录 `CLAUDE.md` 指向本文件,两个工具读取同一份局部规则。

## 职责边界

- **负责**:<本子项目负责的能力>
- **不负责**:<明确不属于本子项目的范围>
- **输入 / 依赖**:<依赖的模块或根级契约路径>
- **输出 / 对外契约**:<对外能力及 `<ROOT_DOCS_PATH>/contracts/` 中的真相源>

## 本模块入口

- **源码**:<路径>
- **测试**:<路径或命令>
- **构建 / 运行**:<命令>
- **关键配置**:<路径>

## 局部约束

- 这里只记录本模块非显而易见的心智模型、内部约定、禁区和模块级踩坑。
- 跨模块约定写入根级 `<ROOT_DOCS_PATH>/contracts/`,并同步受影响模块的局部入口。
- 总体目标、计划、任务卡、决策、问题和交接只写根级 `<ROOT_FLOW_PATH>/`,不在本模块重复建立控制层。
- 项目文档默认集中在根级 `<ROOT_DOCS_PATH>/`;必须紧贴代码维护的 README 或生成文档可留在本模块,并从根级文档索引过去。

## 局部知识(durable,随模块积累)

<!-- 只写看代码看不出的为什么;不要复制根级 AGENTS.md 或一次性进度。 -->
